import { DetailHelper } from './detail-helper';

describe('DetailHelper', () => {
  let detailHelper: DetailHelper;

  it('should create an instance', () => {
    expect(detailHelper).toBeTruthy();
  });
});
